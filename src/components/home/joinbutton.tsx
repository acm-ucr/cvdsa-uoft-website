import React from "react";

const JoinButton = () => {
  return (
    <div className="flex items-center justify-center">
      <button className="mt-[3vh] rounded-full border px-6 py-2 font-seasons text-3xl shadow-lg">
        JOIN NOW!
      </button>
    </div>
  );
};

export default JoinButton;
